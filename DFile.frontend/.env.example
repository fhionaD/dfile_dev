# Local backend (dotnet run profile: http://localhost:5090)
NEXT_PUBLIC_API_URL=http://localhost:5090

# For single-host deployment where ASP.NET serves frontend + API from same domain,
# set this to empty at build time.
# NEXT_PUBLIC_API_URL=
