name: Deploy to MonsterAsp.net

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # 1. Checkout Code
    - name: Checkout code
      uses: actions/checkout@v4

    # 2. Setup Node.js and Build Frontend
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install Frontend Dependencies
      working-directory: ./DFile.frontend
      run: npm ci --legacy-peer-deps

    - name: Build Frontend (Next.js)
      working-directory: ./DFile.frontend
      env:
        # IMPORTANT: Keep this empty for relative paths in "Static Export" mode
        # or use the full URL if you prefer. Relative is safest for same-domain hosting.
        NEXT_PUBLIC_API_URL: "" 
      run: npm run build

    # 3. Setup .NET and Build Backend
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    # Inject production secrets into appsettings.Production.json before publish.
    # Values come from GitHub repository secrets (Settings → Secrets and variables → Actions).
    - name: Write production appsettings
      shell: bash
      env:
        DB_CONNECTION: ${{ secrets.CONNECTIONSTRINGS__DEFAULTCONNECTION }}
        JWT_KEY: ${{ secrets.JWT__KEY }}
        GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE__CLIENTID }}
        GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE__CLIENTSECRET }}
        GOOGLE_BACKEND_BASE_URL: ${{ secrets.APP_BASE_URL }}
        PAYMONGO_SECRET_KEY: ${{ secrets.PAYMONGO__SECRETKEY }}
        PAYMONGO_PUBLIC_KEY: ${{ secrets.PAYMONGO__PUBLICKEY }}
        PAYMONGO_WEBHOOK_SECRET: ${{ secrets.PAYMONGO__WEBHOOKSECRET }}
        PAYMENT_APP_BASE_URL: ${{ secrets.APP_BASE_URL }}
        SMTP_HOST: ${{ secrets.SMTPSETTINGS__HOST }}
        SMTP_PORT: ${{ secrets.SMTPSETTINGS__PORT }}
        SMTP_EMAIL: ${{ secrets.SMTPSETTINGS__EMAIL }}
        SMTP_PASSWORD: ${{ secrets.SMTPSETTINGS__PASSWORD }}
        FRONTEND_ORIGIN: ${{ secrets.APP_BASE_URL }}
      run: |
        cat > DFile.backend/appsettings.Production.json << EOF
        {
          "Logging": {
            "LogLevel": {
              "Default": "Warning",
              "Microsoft.AspNetCore": "Warning"
            }
          },
          "ConnectionStrings": {
            "DefaultConnection": "$DB_CONNECTION"
          },
          "Jwt": {
            "Key": "$JWT_KEY",
            "ExpirationMinutes": 30,
            "Issuer": "DFile",
            "Audience": "DFile"
          },
          "Google": {
            "ClientId": "$GOOGLE_CLIENT_ID",
            "ClientSecret": "$GOOGLE_CLIENT_SECRET",
            "BackendBaseUrl": "$GOOGLE_BACKEND_BASE_URL"
          },
          "PayMongo": {
            "SecretKey": "$PAYMONGO_SECRET_KEY",
            "PublicKey": "$PAYMONGO_PUBLIC_KEY",
            "WebhookSecret": "$PAYMONGO_WEBHOOK_SECRET"
          },
          "Payment": {
            "AppBaseUrl": "$PAYMENT_APP_BASE_URL"
          },
          "SmtpSettings": {
            "Host": "$SMTP_HOST",
            "Port": $SMTP_PORT,
            "Email": "$SMTP_EMAIL",
            "Password": "$SMTP_PASSWORD"
          },
          "AllowedOrigins": {
            "Frontend": "$FRONTEND_ORIGIN"
          }
        }
        EOF

    - name: Publish Backend
      # Framework-dependent publish — relies on the server's installed .NET 8 runtime.
      # MonsterASP.NET FreeSite plan has .NET 8 LTS installed.
      # Produces dfile.backend.dll + web.config in ./publish/ (no bundled runtime = smaller upload).
      run: dotnet publish DFile.backend/dfile.backend.csproj -c Release -o ./publish

    # 4. Verify publish output structure before upload
    - name: Verify publish output
      run: |
        echo "=== publish root (goes to server /wwwroot/) ==="
        ls ./publish/*.dll ./publish/web.config ./publish/appsettings.json
        echo "=== publish/wwwroot (goes to server /wwwroot/wwwroot/) ==="
        ls ./publish/wwwroot/index.html ./publish/wwwroot/_next 2>/dev/null || echo "WARNING: index.html or _next missing"
        echo "Deployed: $(date -u)" > ./publish/deploy-check.txt

    # 5. Take app offline — releases IIS file locks so the .dll can be replaced
    - name: Upload app_offline.htm
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./.github/offline/
        server-dir: /wwwroot/

    # 6. Deploy via FTP (files overwrite in-place; no clean-slate to avoid locked-file errors)
    - name: Deploy to MonsterAsp.net via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./publish/
        server-dir: /wwwroot/

    # 7. Bring app back online — delete app_offline.htm via FTP using Python ftplib
    - name: Remove app_offline.htm (bring app online)
      shell: python3 {0}
      env:
        FTP_SERVER: ${{ secrets.FTP_SERVER }}
        FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
        FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
      run: |
        import ftplib, os
        ftp = ftplib.FTP(os.environ['FTP_SERVER'].strip())
        ftp.login(os.environ['FTP_USERNAME'].strip(), os.environ['FTP_PASSWORD'].strip())
        try:
            ftp.delete('wwwroot/app_offline.htm')
            print('app_offline.htm deleted — site is online')
        except ftplib.error_perm as e:
            print(f'Note: {e}')
        ftp.quit()
